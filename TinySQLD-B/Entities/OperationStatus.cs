namespace Entities
{
    public enum OperationStatus
    {
        Success,
        Error,
        Warning
    }
    public enum ColumnType
    {
        Integer,
        DateTime,
        Double,
        Varchar
    }
}
